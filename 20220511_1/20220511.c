#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {

	int x;
	int N[6] = { 0, }; // 6자리, N[0]:십만 ~ N[5]:일
	int odd[6] = { 0, };
	int odd_count = 0;
	int even[6] = { 0, };
	int even_count = 0;
	int buf;
	//int N100000, N10000, N1000, N100, N10, N1, Buff;
	
	scanf("%d", &x);

	N[0] = x / 100000;
	N[1] = (x - (100000 * N[0])) / 10000;
	N[2] = (x - (100000 * N[0]) - (10000 * N[1])) / 1000;
	N[3] = (x - (100000 * N[0]) - (10000 * N[1]) - (1000 * N[2])) / 100;
	N[4] = (x - (100000 * N[0]) - (10000 * N[1]) - (1000 * N[2]) - (100 * N[3])) / 10;
	N[5] = (x - (100000 * N[0]) - (10000 * N[1]) - (1000 * N[2]) - (100 * N[3]) - (10 * N[4]));
	
	//for(int i = 0 ; i < 6 ; i++)
	//	printf("%d  ", N[i]);

	//N100000 = x / 100000;
	//Buff = x % 100000;
	//N10000 = Buff / 10000;
	//Buff = Buff % 10000;
	//N1000 = Buff / 1000;
	//Buff = Buff % 1000;
	//N100 = Buff / 100;
	//Buff = Buff % 100;
	//N10 = Buff / 10;
	//N1 = Buff % 10;
	//
	//printf("%d %d %d %d %d %d\n", N100000, N10000, N1000, N100, N10, N1);

	//===============================================================================================
	//< 짝수, 홀수 분류 >
	for (int i = 0; i < 6 ; i++)
	{
		if (N[i] % 2 == 1 && N[i] != 0)
		{
			odd[i] = N[i];
			odd_count++;
		}
		else
		{
			even[i] = N[i];
			even_count++;
		}
	}
	//===============================================================================================
	//< 배열 역순으로 만들기 >
	
	//odd
	for (int i = 0; i < odd_count; i++)
	{

	}




}